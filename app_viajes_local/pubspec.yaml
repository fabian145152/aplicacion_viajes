name: tu_app_gps
description: Aplicación de rastreo GPS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.0.0
  wakelock_plus: ^1.1.0
  http: ^1.1.0
  url_launcher: ^6.2.5   # <--- 🔴 AGREGA ESTA LÍNEA
  webview_flutter: ^4.10.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true